node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Sensitive credentials and secrets
*.pem
*.key
*.p12
*.pfx
*.jks
.env
.env.*

# Attached assets (user uploads, pasted content, generated images)
attached_assets/

# Photo cache (large binary files, downloaded at runtime)
photo-cache/

# Replit internals
.cache
.config
.local
